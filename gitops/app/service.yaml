apiVersion: v1
kind: Service
metadata:
  name: open-data-service
  namespace: open-data
spec:
  type: NodePort
  selector:
    app: open-data-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080